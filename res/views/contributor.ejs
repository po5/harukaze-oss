<link rel="stylesheet" href="/static/css/contributor.css">

<h1><%= username %></h1>
<div class="container">
    <img id="avatar" src="<%= avatarUrl %>" alt="<%= username %>'s avatar">
    <div id="bio-container" class="speech-bubble speech-bubble-left">
        <div class="speech-bubble-content">
            <% if(bio) { %>
                <%= bio %>
            <% } else { %>
                <i>No bio added...</i>
            <% } %>
        </div>
    </div>
    <% if(info) { %>
        <br><br>
        <div class="container-header">Info</div>
        <img src="<%= characterUrl %>" alt="<%= username %>'s comment character">
        <div id="info-container" class="speech-bubble speech-bubble-left">
            <div class="speech-bubble-point"></div>
            <div class="speech-bubble-content">
                <%- renderBBCode(info) %>
            </div>
        </div>
    <% } %>
</div>

<div id="stats" class="container">
    <% if(mediaCount > 0) { %>
        <div class="container-header">Booru</div>
        <p><%= username %> has uploaded <%= mediaCount %> item(s) to the booru.</p>
        <p><a href="/booru/user/<%= username %>">View <%= username %>'s booru user page</a></p>
    <% } %>
    <div class="container-header">Posts</div>
    <% if(posts.length > 0) { %>
        <div id="posts-container">
            <% for(post of posts) { with(post) { %>
                <div class="post-listing">
                    <span class="name2"><a href="/blog/<%= slug %>"><%= title %></a>, posted <%= date(created_on) %></span>
                </div>
            <% } } %>
        </div>
    <% } else { %>
        <p><i><%= username %> hasn't written anything yet...</i></p>
    <% } %>
</div>
<% for(var i = 0; i < contributors.length; i++) { %>
    <% var contributor = contributors[i] %>
    <%# to the layman, this might look retarded. that's because it is. thanks george epic css %>
    <% if(i == 0) { %>
        <div class="sidenav">
    <% } else { %>
        <div class="sidenav2">
    <% } %>
        <div class="iconz">
            <img src="/static/img/testicon.png"><div class="nameperson"><a href=/user/<%= contributor.name %>><%= contributor.name %></a></div>
            <div class="sidedesc"><%=contributor.description%></div>
        </div>
    </div>
<% } %>